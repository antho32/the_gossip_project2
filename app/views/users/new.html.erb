<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Créer un compte</title>
  </head>
  <body>
    <% if @user.errors.any? %>
      <div class="alert alert-danger" role="alert">
      <p> Nous n'avons pas réussi à créer le compte pour la (ou les) raison(s) suivante(s) :</p>
      <ul>
        <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
      </div>
    <% end %>
    <br>
    <br>
    <h1><b>Créer un compte</b></h1>

    <%= form_tag url_for(action: 'create'), method: "post" do %>
    <div class="form-group">
      <%= label_tag 'Prénom' %>
      <%= text_field_tag 'first_name', @user.first_name, class: 'form-control' %>
      <div class="form-group">
        <%= label_tag 'Nom' %>
        <%= text_field_tag 'last_name', @user.last_name, class: 'form-control' %>
      <div class="form-group">
        <%= label_tag 'Email*'%>
        <%= text_field_tag 'email', @user.email, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= label_tag 'Mot de passe*' %> <span style="font-size:12px;"> 6 à 20 caractères</span>
        <%= text_field_tag 'password', @user.password, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= submit_tag "Go", class:"btn btn-light" %>
      </div>
    <% end %>

  </body>
</html>

<%= link_to 'Back', users_path %>
